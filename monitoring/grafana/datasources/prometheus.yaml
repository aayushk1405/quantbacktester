# monitoring/grafana/datasources/prometheus.yaml
# Auto-provisioned on Grafana startup — no manual UI setup needed.

apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
